"""Sample-data generators for MedArchive (development / demo only).

Two CLI-runnable generators live here:

  * ``generate_service_catalog`` — writes ``sample_data/service_catalog.{xlsx,json}``
  * ``generate_sample_archive``  — writes ``sample_data/archive.zip`` of synthetic
    Kazakhstan clinic price lists across every supported format.

Shared definitions (services + clinics) live in ``_sampledata_spec``.
"""
